# Gemini AI credentials
# Local dev: copy this file to .env and fill in the values (never commit .env)
# Vercel deploy: set these in Project Settings → Environment Variables
# CI (GitHub Actions): set as repo secrets under Settings → Secrets and Variables
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
